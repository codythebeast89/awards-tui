__pycache__/
*.pyc
.venv/
venv/
*.csv
.DS_Store
credentials.json
client_secret.json
oauth_credentials.json
client_secret*.json
token.json
service_account.json
service-account.json
*.pem
audits/*
!audits/QMC-TRACKER-UPGRADE-GUIDE.md
!audits/image-map.json
audit-*.txt

# Rust
/target/
**/*.rs.bk
.claude/skills/
.specify/

# graphify (knowledge-graph tooling) - graphify-out/ itself is committed so the
# team starts with a shared map; these two are excluded per graphify's own
# recommended .gitignore additions.
# local per-run token-cost log, not shared state
graphify-out/cost.json
# regenerable extraction cache - skip to keep repo small
graphify-out/cache/
